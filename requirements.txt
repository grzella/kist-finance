flask==3.1.3
# Pinned exact: run.sh auto-installs this file with no --require-hashes, so an
# unpinned name would pull "latest" on a fresh box (artifact-substitution risk).
# pytrends upstream (GeneralMills/pytrends) was archived 2025-04-17 — migrate to
# trendspy eventually; lazy-imported (barometer only), not on the web hot path.
pytrends==4.9.2
